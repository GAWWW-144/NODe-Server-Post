<!DOCTYPE html>
<html>
<head>
    <title><%= post.title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav.css">
</head>
<body>
    <%- include('../partials/nav', { title: '상세보기', user: user }) %>
    <div class="container">
        <div style="border-bottom: 2px solid #333; padding-bottom: 10px; margin-bottom: 20px;">
            <h2><%= post.title %></h2>
            <div style="color: #666; font-size: 0.9em;">
                <span>작성자: <%= post.user_name %></span> | 
                <span>조회수: <%= post.view_count %></span> | 
                <span>날짜: <%= post.created_at.toISOString().slice(0,10) %></span>
            </div>
        </div>
        
        <div style="min-height: 200px; padding: 20px; background-color: #f9f9f9; white-space: pre-wrap;"><%= post.content %></div>

        <div style="margin-top: 20px; display: flex; gap: 10px;">
            <a href="/posts?type=<%= post.type %>" class="button">목록</a>
            
            <% if(user.user_id === post.user_id || user.email === 'admin@example.com') { %>
                <a href="/posts/edit/<%= post.post_id %>" class="button" style="background-color: #28a745;">수정</a>
                <a href="/posts/delete/<%= post.post_id %>" class="delete" onclick="return confirm('삭제하시겠습니까?');">삭제</a>
            <% } %>
        </div>
    </div>
</body>
</html>