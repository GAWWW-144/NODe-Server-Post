<!DOCTYPE html>
<html>
<head>
    <title>글쓰기</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/nav.css">
</head>
<body>
    <%- include('../partials/nav', { title: '새 글 작성', user: user }) %>
    <div class="container">
        <h2>글쓰기</h2>
        <form action="/posts/write" method="POST" style="display: flex; flex-direction: column; gap: 15px;">
            <div class="form-group">
                <label>게시판:</label>
                <select name="type" style="padding: 5px;">
                    <option value="free" <%= (typeof post !== 'undefined' && post.type==='free') || type==='free' ? 'selected' : '' %>>자유게시판</option>
    
                     <% if (user.email === 'admin@example.com') { %>
                         <option value="notice" <%= (typeof post !== 'undefined' && post.type==='notice') || type==='notice' ? 'selected' : '' %>>공지사항</option>
                     <% } %>
                </select>
            </div>
            <div class="form-group">
                <input type="text" name="title" placeholder="제목" required style="width: 100%; padding: 10px;">
            </div>
            <div class="form-group">
                <textarea name="content" rows="15" placeholder="내용" required style="width: 100%; padding: 10px;"></textarea>
            </div>
            <div>
                <button type="submit" class="button">등록</button>
                <a href="/posts" class="button" style="background-color: #aaa;">취소</a>
            </div>
        </form>
    </div>
</body>
</html>